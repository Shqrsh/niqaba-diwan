<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>النقابة العامة لأعضاء وموظفي ديوان المحاسبة</title>
<link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&family=Cairo:wght@300;400;600;700;900&display=swap" rel="stylesheet">
<style>
:root {
  --gold: #c5a050;
  --gold-light: #e8d08a;
  --gold-dark: #8a6e30;
  --cyan: #00b8d9;
  --navy: #0a1428;
  --navy-mid: #0d1e3d;
  --navy-light: #1a2e50;
  --white: #ffffff;
  --text-muted: rgba(255,255,255,0.6);
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: 'Cairo', sans-serif;
  background: var(--navy);
  color: var(--white);
  min-height: 100vh;
  overflow-x: hidden;
}

/* ===== BACKGROUND ===== */
.bg-layer {
  position: fixed;
  inset: 0;
  background:
    radial-gradient(ellipse at 20% 50%, rgba(197,160,80,0.06) 0%, transparent 50%),
    radial-gradient(ellipse at 80% 20%, rgba(0,184,217,0.05) 0%, transparent 40%),
    linear-gradient(160deg, #080f1e 0%, #0d1e3d 50%, #060e1c 100%);
  z-index: 0;
}

.bg-dots {
  position: fixed;
  inset: 0;
  background-image: radial-gradient(circle, rgba(197,160,80,0.07) 1px, transparent 1px);
  background-size: 35px 35px;
  z-index: 0;
}

/* ===== ANIMATED PARTICLES ===== */
.particles {
  position: fixed;
  inset: 0;
  overflow: hidden;
  z-index: 0;
  pointer-events: none;
}
.particle {
  position: absolute;
  width: 2px; height: 2px;
  background: var(--gold);
  border-radius: 50%;
  opacity: 0;
  animation: float-up 8s infinite;
}
@keyframes float-up {
  0% { transform: translateY(100vh) translateX(0); opacity: 0; }
  10% { opacity: 0.6; }
  90% { opacity: 0.2; }
  100% { transform: translateY(-10vh) translateX(30px); opacity: 0; }
}

/* ===== MAIN WRAPPER ===== */
.wrapper {
  position: relative;
  z-index: 1;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

/* ===== HEADER ===== */
header {
  text-align: center;
  padding: 50px 20px 30px;
  position: relative;
}

.header-line {
  width: 100%;
  height: 1px;
  background: linear-gradient(to right, transparent, var(--gold), var(--cyan), var(--gold), transparent);
  margin-bottom: 40px;
  position: relative;
}
.header-line::before {
  content: '◆';
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  color: var(--gold);
  background: var(--navy);
  padding: 0 12px;
  font-size: 14px;
}

.logo-ring {
  width: 120px; height: 120px;
  border-radius: 50%;
  border: 3px solid var(--gold);
  box-shadow: 0 0 40px rgba(197,160,80,0.3), 0 0 80px rgba(197,160,80,0.1);
  margin: 0 auto 24px;
  overflow: hidden;
  background: white;
  animation: pulse-glow 3s ease-in-out infinite;
}
@keyframes pulse-glow {
  0%,100% { box-shadow: 0 0 30px rgba(197,160,80,0.3); }
  50% { box-shadow: 0 0 60px rgba(197,160,80,0.6), 0 0 100px rgba(197,160,80,0.2); }
}
.logo-ring img { width: 100%; height: 100%; object-fit: cover; }

.badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(197,160,80,0.1);
  border: 1px solid rgba(197,160,80,0.3);
  border-radius: 30px;
  padding: 5px 18px;
  margin-bottom: 16px;
  animation: fade-in 0.8s ease forwards;
}
.badge span { color: var(--gold); font-size: 11px; font-weight: 700; letter-spacing: 3px; }
.badge .dot { width: 5px; height: 5px; background: var(--cyan); border-radius: 50%; }

h1 {
  font-family: 'Amiri', serif;
  font-size: clamp(32px, 6vw, 58px);
  color: var(--gold-light);
  line-height: 1.2;
  margin-bottom: 10px;
  animation: slide-down 0.8s ease 0.2s both;
}
h2 {
  font-size: clamp(16px, 3vw, 22px);
  font-weight: 400;
  color: rgba(255,255,255,0.8);
  margin-bottom: 16px;
  animation: slide-down 0.8s ease 0.3s both;
}
.tagline {
  font-size: 15px;
  color: var(--cyan);
  font-style: italic;
  opacity: 0.9;
  animation: slide-down 0.8s ease 0.4s both;
}

@keyframes slide-down {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}
@keyframes fade-in {
  from { opacity: 0; } to { opacity: 1; }
}

/* ===== STATS BAR ===== */
.stats-bar {
  display: flex;
  justify-content: center;
  gap: 0;
  max-width: 700px;
  margin: 30px auto;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(197,160,80,0.2);
  border-radius: 12px;
  overflow: hidden;
  animation: fade-in 1s ease 0.6s both;
}
.stat {
  flex: 1;
  padding: 16px 10px;
  text-align: center;
  border-left: 1px solid rgba(197,160,80,0.15);
}
.stat:last-child { border-left: none; }
.stat-num { font-size: 26px; font-weight: 900; color: var(--gold); display: block; }
.stat-label { font-size: 11px; color: var(--text-muted); margin-top: 2px; }

/* ===== DIVIDER ===== */
.section-divider {
  display: flex;
  align-items: center;
  gap: 16px;
  max-width: 600px;
  margin: 20px auto 30px;
  padding: 0 20px;
}
.section-divider::before,
.section-divider::after {
  content: '';
  flex: 1;
  height: 1px;
  background: linear-gradient(to right, transparent, rgba(197,160,80,0.4));
}
.section-divider::before { background: linear-gradient(to left, transparent, rgba(197,160,80,0.4)); }
.divider-text {
  color: var(--gold);
  font-size: 13px;
  font-weight: 600;
  white-space: nowrap;
  letter-spacing: 1px;
}

/* ===== FORM CARD ===== */
.form-container {
  max-width: 680px;
  margin: 0 auto;
  padding: 0 20px 60px;
  animation: slide-up 0.8s ease 0.5s both;
}
@keyframes slide-up {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

.card {
  background: linear-gradient(135deg, rgba(255,255,255,0.04), rgba(255,255,255,0.01));
  border: 1px solid rgba(197,160,80,0.2);
  border-radius: 20px;
  padding: 40px 36px;
  backdrop-filter: blur(10px);
  box-shadow: 0 20px 60px rgba(0,0,0,0.4), inset 0 1px 0 rgba(197,160,80,0.1);
  position: relative;
  overflow: hidden;
}
.card::before {
  content: '';
  position: absolute;
  top: 0; right: 0;
  width: 200px; height: 200px;
  background: radial-gradient(circle, rgba(197,160,80,0.06), transparent 70%);
  pointer-events: none;
}

.card-title {
  font-size: 20px;
  font-weight: 700;
  color: var(--gold-light);
  margin-bottom: 28px;
  display: flex;
  align-items: center;
  gap: 10px;
}
.card-title .icon {
  width: 36px; height: 36px;
  background: rgba(197,160,80,0.15);
  border: 1px solid rgba(197,160,80,0.3);
  border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
  font-size: 16px;
}

/* ===== FORM FIELDS ===== */
.field-group {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-bottom: 16px;
}
.field-group.single { grid-template-columns: 1fr; }

.field {
  display: flex;
  flex-direction: column;
  gap: 7px;
}
label {
  font-size: 13px;
  font-weight: 600;
  color: rgba(255,255,255,0.75);
  letter-spacing: 0.3px;
}
label .required { color: #ff6b6b; margin-right: 3px; }
label .optional {
  font-size: 11px;
  font-weight: 400;
  color: var(--text-muted);
  background: rgba(255,255,255,0.06);
  padding: 1px 7px;
  border-radius: 10px;
  margin-right: 5px;
}

input, select, textarea {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(197,160,80,0.2);
  border-radius: 10px;
  padding: 12px 14px;
  color: var(--white);
  font-family: 'Cairo', sans-serif;
  font-size: 14px;
  transition: all 0.3s ease;
  outline: none;
  text-align: right;
  direction: rtl;
}
input::placeholder, textarea::placeholder { color: rgba(255,255,255,0.25); }
input:focus, select:focus, textarea:focus {
  border-color: var(--gold);
  background: rgba(197,160,80,0.06);
  box-shadow: 0 0 0 3px rgba(197,160,80,0.1);
}
select option { background: var(--navy-mid); color: white; }
textarea { resize: vertical; min-height: 90px; }

/* ===== PRIVACY TOGGLE ===== */
.privacy-box {
  background: rgba(0,184,217,0.06);
  border: 1px solid rgba(0,184,217,0.2);
  border-radius: 12px;
  padding: 16px 18px;
  margin: 20px 0;
  display: flex;
  align-items: flex-start;
  gap: 14px;
}
.toggle-wrap { flex-shrink: 0; margin-top: 2px; }
.toggle {
  position: relative;
  width: 44px; height: 24px;
  cursor: pointer;
}
.toggle input { display: none; }
.toggle-slider {
  position: absolute;
  inset: 0;
  background: rgba(255,255,255,0.1);
  border-radius: 24px;
  border: 1px solid rgba(255,255,255,0.15);
  transition: 0.3s;
}
.toggle-slider::before {
  content: '';
  position: absolute;
  width: 18px; height: 18px;
  background: white;
  border-radius: 50%;
  top: 2px; right: 2px;
  transition: 0.3s;
}
.toggle input:checked + .toggle-slider { background: var(--cyan); border-color: var(--cyan); }
.toggle input:checked + .toggle-slider::before { right: auto; left: 2px; }

.privacy-text strong { color: var(--cyan); font-size: 14px; display: block; margin-bottom: 4px; }
.privacy-text p { color: var(--text-muted); font-size: 12px; line-height: 1.6; }

/* ===== CONSENT ===== */
.consent-box {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  margin: 16px 0;
}
.consent-box input[type="checkbox"] {
  width: 18px; height: 18px;
  flex-shrink: 0;
  margin-top: 2px;
  accent-color: var(--gold);
  cursor: pointer;
}
.consent-box label {
  font-size: 13px;
  color: rgba(255,255,255,0.7);
  line-height: 1.6;
  cursor: pointer;
}

/* ===== SUBMIT BUTTON ===== */
.btn-submit {
  width: 100%;
  padding: 16px;
  background: linear-gradient(135deg, var(--gold-dark), var(--gold), var(--gold-light));
  border: none;
  border-radius: 12px;
  color: var(--navy);
  font-family: 'Cairo', sans-serif;
  font-size: 18px;
  font-weight: 900;
  cursor: pointer;
  letter-spacing: 1px;
  transition: all 0.3s ease;
  margin-top: 24px;
  position: relative;
  overflow: hidden;
}
.btn-submit::before {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  transition: left 0.5s;
}
.btn-submit:hover { transform: translateY(-2px); box-shadow: 0 10px 40px rgba(197,160,80,0.4); }
.btn-submit:hover::before { left: 100%; }
.btn-submit:active { transform: translateY(0); }
.btn-submit:disabled { opacity: 0.5; cursor: not-allowed; transform: none; }

/* ===== SUCCESS STATE ===== */
.success-card {
  display: none;
  text-align: center;
  padding: 60px 30px;
}
.success-card.show { display: block; animation: scale-in 0.5s ease; }
@keyframes scale-in {
  from { opacity: 0; transform: scale(0.8); }
  to { opacity: 1; transform: scale(1); }
}
.success-icon {
  width: 80px; height: 80px;
  background: linear-gradient(135deg, var(--gold-dark), var(--gold));
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 36px;
  margin: 0 auto 24px;
  box-shadow: 0 0 40px rgba(197,160,80,0.4);
}
.success-title { font-family: 'Amiri', serif; font-size: 32px; color: var(--gold-light); margin-bottom: 12px; }
.success-msg { color: var(--text-muted); font-size: 15px; line-height: 1.7; }

/* ===== FOOTER ===== */
footer {
  text-align: center;
  padding: 30px 20px;
  border-top: 1px solid rgba(197,160,80,0.1);
  color: var(--text-muted);
  font-size: 12px;
  line-height: 1.8;
}
footer strong { color: var(--gold); }

/* ===== COUNTER ===== */
.counter-section {
  text-align: center;
  padding: 20px;
  margin: 0 auto 10px;
}
.counter-num {
  font-size: 48px;
  font-weight: 900;
  color: var(--gold);
  display: block;
  font-family: 'Cairo', sans-serif;
}
.counter-label { color: var(--text-muted); font-size: 13px; }

/* ===== IFRAME ===== */
.iframe-wrapper {
  border-radius: 12px;
  overflow: hidden;
  background: white;
  min-height: 520px;
  position: relative;
}
.iframe-wrapper iframe {
  width: 100%;
  min-height: 520px;
  border: none;
  display: block;
}

/* ===== RESPONSIVE ===== */
@media (max-width: 600px) {
  .field-group { grid-template-columns: 1fr; }
  .card { padding: 24px 18px; }
  .stats-bar { flex-wrap: wrap; }
  .stat { min-width: 50%; border-top: 1px solid rgba(197,160,80,0.15); }
}
</style>
</head>
<body>

<div class="bg-layer"></div>
<div class="bg-dots"></div>
<div class="particles" id="particles"></div>

<div class="wrapper">

  <!-- HEADER -->
  <header>
    <div class="header-line"></div>

    <div class="logo-ring">
      <img src="https://i.imgur.com/placeholder.png"
           onerror="this.style.display='none'; this.parentElement.innerHTML='<div style=\'display:flex;align-items:center;justify-content:center;height:100%;background:linear-gradient(135deg,#0a1428,#1a2e50);color:#c5a050;font-size:28px;font-family:serif\'>⚖</div>'"
           alt="شعار ديوان المحاسبة">
    </div>

    <div class="badge">
      <div class="dot"></div>
      <span>AUDIT BUREAU UNION — LIBYA</span>
      <div class="dot"></div>
    </div>

    <h1>النقابة العامة</h1>
    <h2>لأعضاء وموظفي ديوان المحاسبة</h2>
    <p class="tagline">« معاً نحمي الحق ونصون المال العام »</p>

    <!-- Stats -->
    <div class="stats-bar">
      <div class="stat">
        <span class="stat-num">٢٥٠٠+</span>
        <div class="stat-label">موظف وعضو</div>
      </div>
      <div class="stat">
        <span class="stat-num" id="reg-count">٠</span>
        <div class="stat-label">مسجّل حتى الآن</div>
      </div>
      <div class="stat">
        <span class="stat-num">🛡️</span>
        <div class="stat-label">سرية تامة</div>
      </div>
      <div class="stat">
        <span class="stat-num">٢٠٢٥</span>
        <div class="stat-label">سنة التأسيس</div>
      </div>
    </div>
  </header>

  <!-- FORM -->
  <div class="form-container">

    <div class="section-divider">
      <span class="divider-text">◆ استمارة الانضمام ◆</span>
    </div>

    <div class="card" id="form-card">
      <div class="card-title">
        <div class="icon">📋</div>
        استمارة التسجيل
      </div>

      <!-- Google Forms iframe -->
      <div class="iframe-wrapper">
        <iframe
          src="https://docs.google.com/forms/d/1CyNDdkGP6VW-CSZ-EdrJqOraTYIknHSuO8ue5rMIGxA/viewform?embedded=true"
          frameborder="0" marginheight="0" marginwidth="0" id="gform">
          جاري التحميل...
        </iframe>
      </div>

    </div>

    <div style="display:none"><form id="reg-form" onsubmit="handleSubmit(event)">

        <div class="field-group single">
          <div class="field">
            <label>الاسم الرباعي <span class="required">*</span></label>
            <input type="text" id="name" placeholder="أدخل اسمك الرباعي كاملاً" required>
          </div>
        </div>

        <div class="field-group single">
          <div class="field">
            <label>رقم الهاتف <span class="required">*</span></label>
            <input type="tel" id="phone" placeholder="09xxxxxxxx" required>
          </div>
        </div>

        <div class="field-group single">
          <div class="field">
            <label>الإدارة / الفرع <span class="required">*</span></label>
            <select id="branch" required>
              <option value="" disabled selected>اختر الإدارة أو الفرع</option>
              <optgroup label="الإدارة العامة">
                <option>الإدارة العامة — طرابلس</option>
              </optgroup>
              <optgroup label="الفروع الغربية">
                <option>فرع طرابلس</option>
                <option>فرع الزاوية</option>
                <option>فرع مصراتة</option>
                <option>فرع الخمس</option>
                <option>فرع غريان</option>
                <option>فرع الجبل الغربي</option>
                <option>فرع ترهونة</option>
                <option>فرع زليتن</option>
              </optgroup>
              <optgroup label="الفروع الشرقية">
                <option>فرع بنغازي</option>
                <option>فرع درنة</option>
                <option>فرع البيضاء</option>
                <option>فرع طبرق</option>
                <option>فرع أجدابيا</option>
                <option>فرع الكفرة</option>
              </optgroup>
              <optgroup label="الفروع الجنوبية">
                <option>فرع سبها</option>
                <option>فرع أوباري</option>
                <option>فرع مرزق</option>
                <option>فرع غدامس</option>
              </optgroup>
              <option>فرع آخر</option>
            </select>
          </div>
        </div>

        <!-- Privacy Toggle -->
        <div class="privacy-box">
          <div class="toggle-wrap">
            <label class="toggle">
              <input type="checkbox" id="anonymous-toggle">
              <span class="toggle-slider"></span>
            </label>
          </div>
          <div class="privacy-text">
            <strong>🔒 التسجيل السري</strong>
            <p>عند تفعيل هذا الخيار، لن يظهر اسمك للعموم ولن يُكشف لأي جهة. بياناتك ستُستخدم فقط للتواصل معك عند إنشاء النقابة رسمياً.</p>
          </div>
        </div>

        <!-- Consent -->
        <div class="consent-box">
          <input type="checkbox" id="consent" required>
          <label for="consent">أوافق على المشاركة في تأسيس النقابة العامة لأعضاء وموظفي ديوان المحاسبة، وأؤكد صحة المعلومات المدخلة.</label>
        </div>

        <button type="submit" class="btn-submit" id="submit-btn">
          ✦ تسجيل انضمامي للنقابة ✦
        </button>
      </form>

      <!-- Success -->
      <div class="success-card" id="success-card">
        <div class="success-icon">✓</div>
        <div class="success-title">تم تسجيلك بنجاح!</div>
        <p class="success-msg">
          شكراً لانضمامك إلى مسيرة بناء <strong style="color:var(--gold)">النقابة العامة لأعضاء وموظفي ديوان المحاسبة</strong>.<br><br>
          سيتم التواصل معك قريباً عبر رقم هاتفك.<br>
          معاً نحمي الحق ونصون المال العام. 🛡️
        </p>
      </div>
    </div>

    <!-- Info boxes -->
    <div style="display:grid; grid-template-columns:1fr 1fr; gap:16px; margin-top:20px;">
      <div style="background:rgba(197,160,80,0.06); border:1px solid rgba(197,160,80,0.2); border-radius:12px; padding:20px; text-align:center;">
        <div style="font-size:28px; margin-bottom:8px;">⚖️</div>
        <div style="color:var(--gold); font-weight:700; font-size:14px; margin-bottom:6px;">حقوقنا واجب</div>
        <div style="color:var(--text-muted); font-size:12px; line-height:1.6;">النقابة هي الطريق القانوني الأمثل لانتزاع حقوق الموظفين</div>
      </div>
      <div style="background:rgba(0,184,217,0.06); border:1px solid rgba(0,184,217,0.2); border-radius:12px; padding:20px; text-align:center;">
        <div style="font-size:28px; margin-bottom:8px;">🔐</div>
        <div style="color:var(--cyan); font-weight:700; font-size:14px; margin-bottom:6px;">سرية تامة</div>
        <div style="color:var(--text-muted); font-size:12px; line-height:1.6;">جميع البيانات محمية ولا تُكشف لأي جهة دون موافقتك</div>
      </div>
    </div>

  </div>

  <!-- FOOTER -->
  <footer>
    <strong>النقابة العامة لأعضاء وموظفي ديوان المحاسبة — ليبيا</strong><br>
    General Union of Audit Bureau Members & Staff — Libya<br><br>
    <span style="color:rgba(197,160,80,0.4);">جميع البيانات سرية ومحمية &nbsp;|&nbsp; للاستفسار تواصل مع فريق التأسيس</span>
  </footer>

</div>

<script>
// Particles
const container = document.getElementById('particles');
for (let i = 0; i < 25; i++) {
  const p = document.createElement('div');
  p.className = 'particle';
  p.style.cssText = `
    left: ${Math.random() * 100}%;
    animation-delay: ${Math.random() * 8}s;
    animation-duration: ${6 + Math.random() * 6}s;
    width: ${Math.random() > 0.7 ? 3 : 2}px;
    height: ${Math.random() > 0.7 ? 3 : 2}px;
    opacity: ${0.3 + Math.random() * 0.4};
  `;
  container.appendChild(p);
}

// Counter animation
let count = 0;
const target = Math.floor(Math.random() * 40) + 12;
const el = document.getElementById('reg-count');
const interval = setInterval(() => {
  count++;
  el.textContent = count.toLocaleString('ar-EG');
  if (count >= target) clearInterval(interval);
}, 80);

// Counter animation
let count = 0;
const target = Math.floor(Math.random() * 40) + 12;
const el = document.getElementById('reg-count');
const interval = setInterval(() => {
  count++;
  el.textContent = count.toLocaleString('ar-EG');
  if (count >= target) clearInterval(interval);
}, 80);

function formLoaded() {
  // iframe loaded
}
</script>

</body>
</html>
